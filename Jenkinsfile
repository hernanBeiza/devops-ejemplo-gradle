pipeline {
	agent any

	stages {
		stage('Pipeline') {
			steps {
		      	script {
				    stage('build & test') {
				    	//sh './gradle build'
				    }
				    stage('sonar') {
				    	//sh './gradle build'

				    }
				    stage('run') {
				    	//sh './gradle build'

				    }
				    stage('rest') {
				    	//sh './gradle build'

				    }
				    stage('nexus') {

				    }
	      		}
			}
    	}
	}

}
